id = "infinite-loop"
name = "Infinite Loop (test)"
version = "1.0.0"
host_api = "^1"
types = ["highlighter"]
extensions = [".lua"]
publisher = "edit test fixtures"
description = "Deliberately misbehaving plugin used to verify timeout isolation. Do not use."
